# 更新日志 (Changelog)

本项目的所有重要更新都将记录在此文件中。

## [1.1.2] - 2026-06-05

### 🐛 问题修复
*   **彻底修复歌手与专辑的“乱码”与“HTML 实体转义”问题**：
    *   **规避 CSP & Trusted Types 限制**：网易云页面启用了严格的内容安全策略，此前使用内存 DOM 节点赋值 `innerHTML` 解码的方法会触发安全性报错并静默降级为原始未解码状态。重构为纯 JS 解码方案，彻底绕过 CSP 限制。
    *   **完备实体字符解码**：内置了包括 `&middot;`（用于迈克尔·杰克逊等外国歌手的圆点）、`&amp;` 等在内的命名实体，以及十进制/十六进制实体的完整匹配与替换，确保各类名字特殊符号正常渲染。
    *   **精细化 title 属性检索**：有些网易云歌单歌手及专辑的 title 属性被隐藏在子元素（如 `span`、`a`）中，优化解析逻辑向下深度匹配 `[title]`，确保获取到最完整、无 CSS 截断 ellipsis (`...`) 的名称数据。

## [1.1.1] - 2026-06-05

### ✨ 新增功能
*   **独立标签页运行模式 (防自动缩回)**：
    *   在插件弹窗右上角增加了「新标签页打开」图标按钮。
    *   点击可将插件以独立浏览器标签页形式打开。在此模式下，即使频繁切换到网易云音乐页面进行页面滚动或操作，插件页面也绝不会因为失去焦点而自动折叠缩回。
    *   优化了后台标签页查询绑定逻辑：当插件作为独立 Tab 运行时，会自动在所有窗口中探测并精准绑定正在播放或活跃的网易云音乐标签页，确保解析与通信行为与常规弹窗模式完全一致。

### 🎨 界面与体验优化
*   **页面宽高大幅扩展**：将弹窗的默认宽度由 `360px` 增加为 `460px`，列表最大高度从 `160px` 提升至 `260px`，减少眼部疲劳，提升操作舒适度。

### 🐛 问题修复
*   **消除剪贴板粘贴首字乱码 (BOM 冲突)**：重构了数据导出机制，将 UTF-8 BOM 字符 `\ufeff` 分离。剪贴板一键复制时不会附带 BOM，保证在 Notion、Typora 或代码编辑器中粘贴不会出现首字隐藏乱码或显示问号的问题；下载 CSV 文件时依然保留 BOM 标志，以完美兼容 Excel 默认中文解码。
*   **修复歌曲信息解析冗余与错误**：
    *   对于歌曲名：精确定位 `a` 标签中的 `b` 节点，并且智能拼接同级 `.s-fc3` 副标题和翻译文本，彻底过滤掉“MV”等图标产生的干扰文字。
    *   对于歌手与专辑：优先解析 DOM 节点的原生 `title` 属性（如网易云内置的以 `/` 拼接的合唱歌手字符串），避免提取到多余的列表链接标签或拼接错误。

---

## [1.1.0] - 2026-06-04

### ✨ 新增功能
*   **一键复制到剪贴板**：
    *   在插件底部新增「复制到剪贴板」次级按钮。
    *   选中不同导出配置时自动转换对应格式（CSV 带 BOM 表头、JSON 结构体、TXT 带索引列表），点击即可复制到剪贴板，支持在 Excel、Notion 等应用中直接 `Ctrl+V` 粘贴。
*   **弹窗内搜索与实时过滤**：
    *   在歌曲选择卡片顶部增加了一个精致的搜索框。
    *   支持对“歌名”、“歌手”及“专辑”进行不区分大小写的模糊搜索。
    *   在过滤状态下，顶部的“全选”与“反选”操作将**仅作用于当前过滤后可见的歌曲**，使批量分段选择更为精确。
*   **超长歌单自动滚动与懒加载解决**：
    *   引入了异步自动滚动机制。当用户点击一键解析时，插件内容脚本会自动以 350ms 为间隔向下探底滚动，直到所有懒加载的歌曲行完全加载完毕（行数连续两次检测无变化）再触发解析，保证抓取 100% 完整。

### 🎨 界面与体验优化
*   **页脚布局重构**：页脚按钮区由原有的单按钮宽度升级为双按钮网格排布（`btnExport` 与 `btnCopy` 按 1.25:1 比例自动分配宽度），界面布局更合理。
*   **成功微交互反馈**：在执行下载或复制成功后，按钮会伴随微动画平滑过渡为绿色成功态，并短暂显示“已下载！”或“已复制！”提示，提升使用温度感。

---

## [1.0.0] - 2026-05-31

*   **初始版本发布**：
    *   支持网易云音乐歌单、排行榜、专辑页面的歌曲、歌手、专辑数据解析。
    *   支持 CSV、JSON、TXT 格式导出与浏览器本地免权限静默下载。
    *   支持歌曲勾选与反选、全选功能。
    *   暗黑极客风 UI 设计与 Bicubic 算法缩放的 3D 定制图标。
